# گزارش تطبیق نیازمندی‌های MVP با کدهای موجود سیستم PlayeNest

این گزارش بر اساس تحلیل کامل دیتابیس (`prisma/schema.prisma`)، APIها، سرویس‌ها و کنترلرهای پروژه PlayeNest در مقایسه با سند نیازمندی‌های MVP (ارائه شده توسط مؤذن) تنظیم شده است.

---

## ۱. پاسخ اختصاصی به وضعیت سیستم تخفیف (Discount System)

### ❌ عدم وجود قابلیت ایجاد تخفیف روی گیم‌نت یا دستگاه

* **وضعیت در دیتابیس**: در جدول `Reservation` فقط دو فیلد `discountAmount` (برای ثبت مبلغ تخفیف عددی روی رزرو) و `voucherCode` وجود دارند.
* **کمبود اصلی در معماری**: هیچ مدل `Discount` در فایل `prisma/schema.prisma` تعریف نشده است.
* **جزئیات موارد پیاده‌سازی‌نشده**:
  1. امکان تعریف تخفیف‌های Generic (تخفیف روی کل یک گیم‌نت یا یک دستگاه خاص مانند PS5) وجود ندارد.
  2. فیلدهای کلیدی مدل تخفیف پیشنهادی شامل `target`, `target_id`, `value_type` (درصدی یا مبلغ ثابت)، `value`, `start_at`, `end_at` و `is_active` موجود نیستند.
  3. هیچ مسیر (Route) و کنترلری در پنل بک‌آفیس (Backoffice) یا پنل ادمین برای ایجاد، مشاهده، ویرایش یا فعال/غیرفعال‌سازی تخفیف پیاده‌سازی نشده است.

---

## ۲. وضعیت ارسال پیامک اطلاع‌رسانی به صاحب گیم‌نت (Owner SMS Notification)

### ⚠️ ناقص / عدم ارسال پیامک رزرو به ادمین گیم‌نت

* **توضیحات**: سرویس ارسال پیامک (`src/modules/notifications/sms.station.ts`) و رویدادهای رزرو (`src/modules/reservation/reservation.events.ts`) در سیستم موجود هستند.
* **مورد پیاده‌سازی‌شده**: هنگام ثبت رزرو جدید (`RESERVATION_CREATED`) پیامک تایید/درخواست رزرو فقط برای **مشتری** (`customerAccount.phone`) ارسال می‌شود.
* **کمبود اصلی**: ارسال پیامک خودکار به **صاحب گیم‌نت / ادمین** (با متنی مانند «یک درخواست رزرو جدید برای گیم‌نت شما ثبت شده است...») پیاده‌سازی نشده است.

---

## ۳. تحلیل ساختار دستگاه‌ها (Device Type vs Physical Device)

### ⚠️ تفاوت ساختاری با الگوی پیشنهادی مؤذن

* **ساختار فعلی سیستم**: در مدل دیتابیس (`GameStation`)، مفهوم دستگاه فیزیکی و نوع آن با هم ترکیب شده‌اند.
* **توضیحات**: هر `GameStation` مستقیماً نمایانگر یک دستگاه واقعی (مثلاً "دستگاه PS5 شماره ۱") است که فیلد `stationType` (شامل `PC`, `PLAYSTATION`, `XBOX`, `SWITCH`, `VR`) و قیمت ساعتی `hourlyPrice` روی خود آن تعریف شده است.
* **تفاوت**: ساختار دو لایه‌ای مجزا (یک جدول `DeviceType` برای تعیین قیمت و تعریف نوع، و جدول متصل `PhysicalDevice` برای رزرو دستگاه‌های واقعی) وجود ندارد؛ رزروها و قیمت‌گذاری‌ها به صورت مستقیم روی `GameStation` انجام می‌شوند.

---

## ۴. جدول تطبیق کامل صفحات و قابلیت‌های MVP

### 👤 ۱. اپلیکیشن / پنل کاربر (User Panel & Public APIs)

| دسته / صفحه | قابلیت‌های اصلی | اولویت | وضعیت در کدها | توضیحات / ماژول پیاده‌سازی |
| :--- | :--- | :---: | :---: | :--- |
| **ثبت‌نام / ورود** | ثبت‌نام، ورود، OTP یا رمز عبور، خروج | 🔴 ضروری | ✅ موجود | ماژول `auth` (`/api/v1/auth/*`) همراه با پیامک OTP |
| **صفحه اصلی** | نمایش گیم‌نت‌ها، جدیدترین‌ها، نزدیک‌ترین‌ها، جستجو | 🔴 ضروری | ✅ موجود | ماژول‌های `public` و `cms` |
| **جستجو و فیلتر** | جستجو بر اساس نام/منطقه، نوع دستگاه، ویژگی | 🔴 ضروری | ✅ موجود | پارامترهای فیلتر در روت‌های عمومی گیم‌نت |
| **لیست گیم‌نت‌ها** | کارت گیم‌نت، عکس، نام، فاصله، ویژگی‌ها، قیمت شروع | 🔴 ضروری | ✅ موجود | کنترلر عمومی `public.controller.ts` |
| **جزئیات گیم‌نت** | عکس‌ها، آدرس، ساعت کاری، امکانات، دستگاه‌ها، قیمت‌ها | 🔴 ضروری | ✅ موجود | ماژول‌های `cms`, `public`, `station` |
| **انتخاب دستگاه** | مشاهده دستگاه‌های موجود، نوع دستگاه، قیمت ۱ ساعت | 🔴 ضروری | ✅ موجود | ماژول `station` (`/stations`) |
| **انتخاب تاریخ و زمان** | انتخاب تاریخ، ساعت شروع/پایان، نمایش زمان‌های آزاد | 🔴 ضروری | ✅ موجود | ماژول `availability` |
| **ثبت رزرو** | نمایش خلاصه رزرو و ارسال درخواست به ادمین | 🔴 ضروری | ✅ موجود | `POST /public/gamingCenters/:slug/reservation` |
| **وضعیت رزرو** | PENDING, CONFIRMED, CANCELED, IN_PROGRESS, COMPLETED | 🔴 ضروری | ✅ موجود | استیت ماشین رزرو (`ReservationStateMachine`) |
| **تأیید زمان پیشنهادی** | مشاهده زمان پیشنهادی ادمین و قبول/رد آن | 🔴 ضروری | ⚠️ ناقص | وضعیت‌های استیت ماشین شامل PENDING, CONFIRMED, IN_PROGRESS, COMPLETED, CANCELED, NO_SHOW است اما جریان اختصاصی Reschedule/Propose New Time مجزا نشده است. |
| **رزروهای من** | لیست رزروهای قبلی و فعلی + جزئیات | 🔴 ضروری | ✅ موجود | ماژول `customer-panel` (`GET /customer/reservation`) |
| **جزئیات رزرو** | گیم‌نت، دستگاه، تاریخ، ساعت، قیمت، وضعیت | 🔴 ضروری | ✅ موجود | `GET /customer/:reservationId` |
| **لغو رزرو** | امکان لغو رزرو طبق قوانین سیستم | 🟡 مهم | ✅ موجود | `POST /customer/reservation/:reservationId/cancel` |
| **اعلان‌ها** | اطلاع از تأیید، رد یا پیشنهاد زمان جدید | 🟡 مهم | ⚠️ فقط SMS | ارسال پیامک وضعیت رزرو به مشتری فعال است. |
| **پروفایل** | اطلاعات حساب، شماره موبایل و خروج | 🟡 مهم | ✅ موجود | `GET /customer/me` |

---

### 🏢 ۲. پنل ادمین / صاحب گیم‌نت (Gaming Center Owner Panel)

| دسته / صفحه | قابلیت‌های اصلی | اولویت | وضعیت در کدها | توضیحات / ماژول پیاده‌سازی |
| :--- | :--- | :---: | :---: | :--- |
| **ورود** | ورود صاحب گیم‌نت | 🔴 ضروری | ✅ موجود | نقش‌های `MANAGER`, `SUPERVISOR`, `STAFF` در ماژول `auth` |
| **داشبورد** | رزروهای جدید، رزروهای امروز، Pending، Confirmed | 🔴 ضروری | ✅ موجود | ماژول `analytics` و فیلتر لیست رزروها |
| **رزروهای جدید** | مشاهده درخواست‌های جدید | 🔴 ضروری | ✅ موجود | `GET /gamingCenters/:id/reservation` |
| **جزئیات رزرو** | کاربر، دستگاه، تاریخ، ساعت، اطلاعات رزرو | 🔴 ضروری | ✅ موجود | ماژول `reservation` |
| **مدیریت رزرو** | تأیید، رد، لغو، پیشنهاد زمان جدید | 🔴 ضروری | ⚠️ نیمه‌کامل | تأیید، رد و لغو فعال است (`PATCH .../status`)؛ اما «پیشنهاد زمان جدید» استیت اختصاصی ندارد. |
| **Calendar** | مشاهده رزرو دستگاه‌ها بر اساس تاریخ و ساعت | 🟡 مهم | ✅ موجود | کوئری‌های فیلتر بازه زمانی در API رزروها |
| **اطلاعات گیم‌نت** | نام، توضیحات، آدرس، تلفن، ساعت کاری، امکانات | 🔴 ضروری | ✅ موجود | ماژول‌های `gamingCenter` و `settings` |
| **تصاویر گیم‌نت** | آپلود چند عکس از محیط و دستگاه‌ها | 🔴 ضروری | ✅ موجود | ماژول `media` و CMS media |
| **مدیریت دستگاه‌ها** | افزودن، حذف و ویرایش دستگاه | 🔴 ضروری | ✅ موجود | ماژول `station` (`/gamingCenters/:id/stations`) |
| **قیمت دستگاه‌ها** | تعیین قیمت یک ساعت برای هر دستگاه | 🔴 ضروری | ✅ موجود | فیلد `hourlyPrice` در `GameStation` |
| **وضعیت دستگاه** | فعال/غیرفعال کردن دستگاه | 🔴 ضروری | ✅ موجود | فیلد `isActive` در `GameStation` |
| **پروفایل** | اطلاعات صاحب گیم‌نت و حساب | 🟡 مهم | ✅ موجود | ماژول `users` |
| **اعلان رزرو** | SMS برای صاحب گیم‌نت بر روی رزرو جدید | 🔴 ضروری | ❌ عدم وجود | پیامک رویداد ثبت رزرو فعلاً فقط به مشتری ارسال می‌شود. |

---

### ⚙️ ۳. پنل تیم فنی / بک‌آفیس (Team Backoffice)

| دسته / صفحه | قابلیت‌های اصلی | اولویت | وضعیت در کدها | توضیحات / ماژول پیاده‌سازی |
| :--- | :--- | :---: | :---: | :--- |
| **Login** | ورود تیم فنی/مدیریت سیستم | 🔴 ضروری | ✅ موجود | نقش `ADMIN` در ماژول `auth` |
| **Dashboard** | آمار کلی سیستم و رزروها | 🟡 مهم | ✅ موجود | ماژول‌های `analytics`, `commissions`, `audit` |
| **Gaming Centers** | مشاهده و مدیریت گیم‌نت‌ها | 🔴 ضروری | ✅ موجود | ماژول `gamingCenter` |
| **Users** | مشاهده کاربران سیستم | 🟡 مهم | ✅ موجود | ماژول `users` |
| **Reservations** | مشاهده تمام رزروهای سیستم | 🟡 مهم | ✅ موجود | ماژول `reservation` |
| **Discount Management** | ایجاد و مدیریت تخفیف | 🔴 ضروری | ❌ **عدم وجود** | **هیچ مدل دیتابیس یا API برای تخفیف پیاده‌سازی نشده است.** |
| **تخفیف گیم‌نت** | اعمال تخفیف روی کل یک گیم‌نت | 🔴 ضروری | ❌ **عدم وجود** | **پیاده‌سازی نشده است.** |
| **تخفیف دستگاه** | اعمال تخفیف روی یک نوع/دستگاه خاص | 🔴 ضروری | ❌ **عدم وجود** | **پیاده‌سازی نشده است.** |
| **فعال/غیرفعال تخفیف** | تعیین وضعیت تخفیف (`is_active`) | 🔴 ضروری | ❌ **عدم وجود** | **پیاده‌سازی نشده است.** |
| **تاریخ تخفیف** | تعیین `start_at` / `end_at` | 🟡 مهم | ❌ **عدم وجود** | **پیاده‌سازی نشده است.** |
| **درصد/مبلغ تخفیف** | تعیین درصد یا مبلغ تخفیف | 🔴 ضروری | ❌ **عدم وجود** | **پیاده‌سازی نشده است.** |

---

## ۵. جمع‌بندی کلی و پیشنهاد فنی

1. **هسته رزرو آنلاین، مدیریت گیم‌نت و دستگاه‌ها، احراز هویت و پنل مشتری**: به شکل جامع و مطابق استانداردهای معماری در پروژه پیاده‌سازی شده است.
2. **مدیریت تخفیف (Discount System)**: کاملاً غایب است و برای افزودن آن نیاز به اضافه کردن مدل `Discount` در Prisma، انجام Migration، و ایجاد ماژول مدیریت تخفیف در پنل Backoffice و اعمال محاسبه تخفیف در ثبت رزرو می‌باشد.
3. **اطلاع‌رسانی پیامکی ادمین**: اضافه کردن ارسال پیامک به صاحب گیم‌نت هنگام ثبت رزرو جدید، با اضافه کردن یک تابع ساده در `reservation.events.ts` امکان‌پذیر است.
